{
  "name": "workflow_devserver",
  "version": "1.0.0",
  "description": "sass envi and flow",
  "main": "app.js",
  "scripts": {
    "start": "gulp",
    "update:packages":"node wipe-dependcies.js && rm -rf node_modules && npm update --save-dev && npm update --save"
  },
  "author": "Smith Similien",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.24.6",
    "gulp": "^3.9.1",
    "gulp-sass": "^4.0.1"
  },
  "dependencies": {
    "lodash": "^4.17.10"
  }
}
